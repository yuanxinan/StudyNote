scoket访问速度比http快，因为http通信除了传输的数据还有消息头什么的。
scoket属于传输层，它是对tcp/ip协议的实现，包含tcp/udp，它是所有通信协议的基础，http协议需要scoket支持，以scoket为基础。

scoket通信特点：
	1.开启接口，该通信是长连接的通信，很容易被防火墙拦截，可以通过心跳机制来实现，开发难度偏大。
	2.传输的数据一般是字符串，可读性不强
	3.socket端口不便于推广（ip.端口）不方便记
	4.性能相对于其他的通信协议是最优的
	